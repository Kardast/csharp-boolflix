@using csharp_boolflix.Models.Form;
@model FilmForm

<div class="container">
    <div class="row">
        <div class="col-12">
            <div class="d-flex align-items-center">
                <h1 class="mt-3 mb-3">Nuova pizza</h1>
            </div>

            <form asp-controller="Film" asp-action="Create" method="post">

                @Html.AntiForgeryToken()

                <div class="mb-3">
                    <strong>Nome:</strong>
                    <input asp-for="Film.Title" class="d-block w-100" />
                    <span asp-validation-for="Film.Title" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <strong>Descrizione:</strong>
                    <textarea asp-for="Film.Description" rows="10" class="d-block w-100"></textarea>
                    <span asp-validation-for="Film.Description" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <strong>Anno:</strong>
                    <textarea asp-for="Film.Year" class="d-block w-100"></textarea>
                    <span asp-validation-for="Film.Year" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <strong>Immagine:</strong>
                    <input asp-for="Film.Image" class="d-block w-100" />
                    <span asp-validation-for="Film.Image" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <strong>Duration:</strong>
                    <input asp-for="Film.Duration" class="d-block w-100" />
                    <span asp-validation-for="Film.Duration" class="text-danger"></span>
                </div>
                <div class="mb-3">
                    <strong>Regista:</strong>
                    <select asp-for="Film.DirectorId" rows="5" class="d-block w-100">
                        @foreach (Director director in Model.Directors)
                        {
                            @if (Model.Film.DirectorId == director.Id)
                            {
                                <option value="@director.Id" selected>@director.Name</option>
                            }
                            else
                            {
                                <option value="@director.Id">@director.Name</option>
                            }
                        }
                    </select>
                </div>
                <div class="mb-3">
                    <strong>Attori:</strong>
                    <select asp-for="SelectedActors"
                            asp-items="@Model.Actors"
                            multiple="multiple"
                            size="5"
                            class="form-control mb-2 mr-sm-2 ml-sm-3 leftList">
                    </select>
                </div>
                <div class="mb-3">
                    <strong>Categorie:</strong>
                    <select asp-for="SelectedGenres"
                            asp-items="@Model.Genres"
                            multiple="multiple"
                            size="5"
                            class="form-control mb-2 mr-sm-2 ml-sm-3 leftList">
                    </select>
                </div>

                <div class="text-end">
                    <input type="submit" class="btn btn-small btn-success" value="Salva">
                </div>

            </form>
        </div>
    </div>
</div>